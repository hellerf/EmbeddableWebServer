By default, this file is a standalone program with the demo functionality. To try the functionality you can do:
gcc -pthread EmbeddableWebServer.h -o EmbeddableWebServer && EmbeddableWebServer

To embed the demo functionality in another program:

#define EWS_DEFINE_MAIN 0
#include "EmbeddableWebServer.h"

int main(int argc, char* argv[]) {
	acceptCon
}

